cvs:
  git:
    main_branch: 'main'
  github:
    repository: 'Vandebron/pympl'
docker:
  registry:
    host_name: 'bigdataregistry.azurecr.io'
    user_name: '${DOCKER_REGISTRY_USERNAME:-user}'
    password: '${DOCKER_REGISTRY_PASSWORD:-password}'
