build:
  run:
    id: 'id'
    run_url: !ENV ${RUN_DISPLAY_URL:http://localhost/run}
    change_url: !ENV ${GIT_URL:http://localhost:3000/}
    tests_url: !ENV ${RUN_TESTS_DISPLAY_URL:http://localhost/tests}
    user: !ENV ${BUILD_USER:somebody}
    user_email: !ENV ${BUILD_USER_EMAIL}
  parameters:
    deploy_target: !ENV ${DEPLOY_TARGET:PullRequest}
  versioning:
    revision: "2ad3293a7675d08bc037ef0846ef55897f38ec8f"
    branch: "feature/ARC-123-branch"
    pr_number: '1234'
    tag: !ENV ${TAG_NAME}
  console:
    width: 0
    logLevel: INFO
stages:
  - name: 'build'
    icon: '🏗️'
  - name: 'test'
    icon: '📋'
  - name: 'deploy'
    icon: '🚀'
  - name: 'postdeploy'
    icon: '➡️'
