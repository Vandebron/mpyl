$schema: http://json-schema.org/draft-07/schema#
$id: run_properties.yml
definitions:
  stageNames:
    enum:
      - "firstStage"
      - "secondStage"
  dependencies:
    type: object
    properties:
      firstStage:
        type: array
        minItems: 1
    additionalProperties: false