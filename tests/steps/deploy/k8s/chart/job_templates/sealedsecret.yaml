apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name:
  labels:
    chart: "job-0.1.0"
  annotations:
    # the namespace will not be included to salt the secret
    # therefore they can be used in any namespace
    sealedsecrets.bitnami.com/cluster-wide: "true"
spec:
  encryptedData: