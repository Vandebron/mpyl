name: job
description: "A job"
maintainer: [ "MPyL" ]
stages:
  build: "Docker Build"
  test: "Echo Test"
  deploy: "Kubernetes Job Deploy"
deployment:
  kubernetes:
    job:
      cron:
        schedule: "0 22 * * *"
dependencies:
  test:
    - tests/projects/service/file.py
