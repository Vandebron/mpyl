# syntax=docker/dockerfile:1.4
FROM node:12.18.2 AS installer
COPY --link tests/projects/caching-service/service/ /tests/projects/caching-service/service/
WORKDIR /tests/projects/service
RUN yarn install

FROM installer as builder
ENTRYPOINT [ "yarn", "start" ]

FROM installer as tester
RUN mkdir -p /tests/projects/caching-service/service/target/test-reports/
RUN yarn test-ci