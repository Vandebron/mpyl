name: 'testredis'
description: 'A test project for redis'
maintainer: [ 'Marketplace', 'Energy Trading' ]
stages:
  deploy: 'Redis Cluster Deploy'
deployment:
  namespace: 'caching-service'
  kubernetes:
    resources:
      instances:
        all: 1
